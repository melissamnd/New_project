[tool.poetry]
name = "new_project"
version = "0.1.0"
description = "adding functionnalities"
authors = ["Melissa Mesnard"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
